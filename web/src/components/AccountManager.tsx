"use client";
import {useActionState} from "react";
import {manageAccount} from "@/lib/account-actions";
import {ROLES} from "@/lib/permissions";
export function AccountManager({accounts,profiles}:{accounts:{id:string;email:string}[];profiles:{id:string;name:string}[]}){
 const [state,action,pending]=useActionState(manageAccount,{ok:false,message:""});
 return <div className="panel"><h2 className="mb-5 text-lg font-semibold">Manage access</h2><form action={action} className="grid gap-4 sm:grid-cols-2"><label className="field">Action<select name="kind" required>{[{v:"create",l:"Create account"},{v:"reset",l:"Reset password"},{v:"disable",l:"Disable account"},{v:"enable",l:"Enable account"},{v:"role",l:"Change role / profile"}].map(o=><option key={o.v} value={o.v}>{o.l}</option>)}</select></label><label className="field">Existing account<select name="userId"><option value="">Select for changes</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.email}</option>)}</select></label><label className="field">Email for new account<input name="email" type="email" maxLength={254}/></label><label className="field">Role<select name="role">{ROLES.map(r=><option key={r} value={r}>{r}</option>)}</select></label><label className="field">Linked profile<select name="partyId"><option value="">None (staff only)</option>{profiles.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="field">Temporary password (create / reset)<input name="password" type="password" minLength={12} maxLength={128} autoComplete="new-password"/></label><div className="sm:col-span-2"><p className="mb-4 text-xs leading-6 text-slate-500">Students and trainers require a matching profile. Access changes revoke existing sessions. Temporary passwords must contain at least 12 characters; share them privately.</p><button className="primary w-full sm:w-auto" disabled={pending}>{pending?"Saving...":"Save account"}</button>{state.message&&<p role="status" className={`mt-3 text-sm ${state.ok?"text-green-700":"text-red-700"}`}>{state.message}</p>}</div></form></div>;
}
